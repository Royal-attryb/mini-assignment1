{
    "version": 2,
    "builds": [
      {
        "src": "server.js",
        "use": "@now/node"
      }
    ],
    "routes": [
      { "src": "/", "dest": "/server.js" },
      { "src": "/get_cars", "dest": "/server.js" },
      { "src": "/get_car", "dest": "/server.js" }
    ]
  }